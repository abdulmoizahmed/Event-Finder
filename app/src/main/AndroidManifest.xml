<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appswallet.jamatfinder">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <receiver android:name=".Utils.AlarmReceiver"> </receiver>
        <receiver android:name=".Utils.JamatReceiver"> </receiver>
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCXF9olyO3TYL7Z76Jn-9pVlfR5bx32SIY" />

        <activity android:name=".Screens.MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>

        <service
            android:name=".Network.FetchAddressIntentService"
            android:exported="false" />

        <activity android:name=".Screens.Addjamat" />
        <activity
            android:name=".firebase_push_notification.auth_screens.SignupScreen"
            android:label="@string/title_activity_signup"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".firebase_push_notification.auth_screens.ResetPasswordScreen"
            android:label="@string/title_activity_reset_password"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".firebase_push_notification.auth_screens.LogInScreen"
            android:label="@string/title_activity_profile_screen"
            android:theme="@style/AppTheme.NoActionBar"></activity>



        <service android:name=".firebase_push_notification.services.FirebaseMessageService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".Screens.Test">

        </activity>

        <service
            android:name=".Utils.AlarmService"
            android:enabled="true"
            android:exported="true"></service>

    </application>

</manifest>